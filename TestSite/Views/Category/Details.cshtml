@model TestSite.Entity.Category

@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
    <div class="container text-center">
        <h2>Detaylar <i class="fas fa-info-circle"></i></h2>

        <div>
            <hr />
            <dl class="dl-horizontal">
                <dt>
                    @Html.DisplayNameFor(model => model.Name)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.Name)
                </dd>

                <dt>
                    @Html.DisplayNameFor(model => model.Description)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.Description)
                </dd>

            </dl>
        </div>
        <p>
            @Html.ActionLink("<-- Back to List", "Index",null,new {@class="btn btn-outline-dark" }) |
            @Html.ActionLink("Edit", "Edit", new { id = Model.Id },new {@class="btn btn-outline-warning" })

        </p>
    </div>